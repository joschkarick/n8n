# Ausdruecklich mitgefuehrt: Der Mealie-MCP-Server braucht mcp.server.fastmcp,
# das es erst ab 1.2 gibt. Ohne diese Zeile haengt die Version allein an der
# Aufloesung des Server-Pakets - und eine zu alte bricht erst zur Laufzeit.
mcp[cli]>=1.12

uvicorn[standard]>=0.30
httpx>=0.27
PyJWT[crypto]>=2.8
